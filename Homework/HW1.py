# Implement the dot product of two vectors, making sure that it is properly defined for the two input vectors

# Create a perceptron class
# The constructor should take an integer n as input, where is the number of inputs into the perceptron
# It should also consist of a "bias" or "threshold" and n weights, one for each input.

# Implement the derivative.
# It should be a function which takes as in put a function f, a value x, and a paramter h.

# Implement the gradient of a potentially multivariable function f with paramter h
